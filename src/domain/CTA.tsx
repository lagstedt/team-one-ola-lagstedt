/**
 * CTA interface represents the structure of the call-to-action button.
 *
 * @typedef {Object} CTA
 * @property {string} label - The label for the button.
 * @property {string} link - The URL the button links to.
 */

interface CTA {
    label: string;
    link: string;
}

export default CTA;